-> T26 Phase 2: object-schemas are open/structural (Decision 3 — an object
-> satisfying a schema may have EXTRA fields), but a REQUIRED field that's
-> simply missing must still be refused.
K = { k ∈ String, m ∈ Number }
mm ∈ K
mm = { k = "x" }
