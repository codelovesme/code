-> Handler default return: handlers without return return Null
Ping = Particle ∩ { _class ∈ "Ping" }

Ping => {
    -> no return statement
    x = 42
}

emit Ping{} to this get result
assert result = Null
