-> T26: `loop <var> { }` requires a FINITE domain. An unbounded range has
-> infinitely many candidates — enumerating it can't ever finish. Must be
-> rejected outright, not hang trying.
x > 0
loop x {
    assert x > 0
}
