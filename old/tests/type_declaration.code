-> Particle type declaration and typed particle constructor
Log = Particle ∩ { _class ∈ "Log", message ∈ String, level ∈ String }

p = Log { message = "Hello World", level = "Info" }
assert p._class = "Log"
assert p.message = "Hello World"
assert p.level = "Info"
