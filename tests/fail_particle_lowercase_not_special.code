-- A lowercase name followed by `{` is not particle syntax — it parses
-- exactly as it always has (a parse error), because `{` is never valid in
-- postfix position and a statement can't just trail extra tokens.
let foo = 1
let x = foo { "a": 1 }
