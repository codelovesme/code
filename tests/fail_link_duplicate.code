| Linking the same file twice runs its top level twice and registers its
| handlers twice — and one handler per class is a program-wide rule, so the
| second link is an error rather than a silent override.
link "modules/isolated_world"
link "modules/isolated_world"
