| `$name` inside a double-quoted string splices the variable's text in.
| A string with no `$` is untouched — it stays a plain literal.
let name = "world"

assert "hi $name" = "hi world"
assert "$name" = "world"
assert "no dollars here" = "no dollars here"
