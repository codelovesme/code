-> `base` target: dispatch to the module that linked this module

Ping = Particle ∩ { _class ∈ "Ping", value ∈ Number }

Ping => {
    return Ping { value = value + 10 }
}

link modules/base_child_single
assert base_result.value = 11
