| A longer loop. Any cycle at all is rejected, not only the direct kind.
A {} => {
    emit B to this get r
    return Ack {}
}
B {} => {
    emit C to this get r
    return Ack {}
}
C {} => {
    emit A to this get r
    return Ack {}
}
emit A to this
