| Ordering is Number-only. Two strings are a matched pair of kinds and there
| is an obvious lexicographic answer, but `<` `>` `≤` `≥` still reject them —
| ordering means numeric ordering here, nothing else. Equality is unaffected:
| `"abc" = "abd"` is perfectly fine, and false.
let x = "abc" < "abd"
