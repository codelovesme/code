| A linked module that sends upward: `to base` reaches whoever linked *this*
| file and answers from that file's own top-level handlers. The answer is
| checked here, since a module's names never leave it.
emit Ping { value = 1 } to base get reply
assert reply.value = 11
