-> Module that defines a handler for Event
Event = Particle ∩ { _class ∈ "Event", name ∈ String }
EventAck = Particle ∩ { _class ∈ "EventAck", ok ∈ Number }

Event => {
    return EventAck{ ok = 1 }
}
