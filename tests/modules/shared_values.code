| A module with nothing but names. They are its own — a module answers
| particles, not names — so the only thing linking this file proves is that
| its top level ran, which it says here itself.
let greeting = "hello"
let n = 42
let derived = n + 1
assert greeting = "hello"
assert derived = 43
