| A module. Nothing here is visible to a linking file unless it says
| `export` — private is the default, the opposite of the old language.
export let greeting = "hello"
export let n = 42

let hidden = 1
let derived = hidden + n
