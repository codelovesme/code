-- Only two objects merge. There is no key to file a bare value under, so an
-- object plus a non-object (and non-array — one array operand makes the
-- other an element) is a type error, not a silent no-op.
let x = { "a": 1 } + 3
