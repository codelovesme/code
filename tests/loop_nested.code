-- Nested loops: the inner loop runs in full for each outer element.
let pairs = []
loop a over [1, 2] {
    loop b over [10, 20] {
        pairs = pairs + [a + b]
    }
}
assert pairs = [11, 21, 12, 22]
