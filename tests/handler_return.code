-> Handler return: handler returns a value via return
Ping = Particle ∩ { _class ∈ "Ping" }
ACK = Particle ∩ { _class ∈ "ACK" }

Ping => {
    return ACK{}
}

emit Ping{} to this get result
assert result ∈ ACK
assert result ≠ Null
