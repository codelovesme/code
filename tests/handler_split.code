-> Split handler definition: type declared separately, handler declared later
type Alert { text ∈ String }
type Ack { ok ∈ Number }

Alert => {
    assert text = "fire"
    return Ack{ ok = 1 }
}

emit Alert{text = "fire"} to this get result
assert result.ok = 1
