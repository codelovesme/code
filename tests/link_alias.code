-- `as name` gathers the exports into an object instead, so they are reached
-- by ordinary field access — no new lookup rule, the alias is just a value.
link "modules/shared_values" as shared

assert shared.greeting = "hello"
assert shared.n = 42
assert shared = { greeting = "hello", n = 42 }
