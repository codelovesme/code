# Rust build artifacts
/target/
**/target/

# Native-module test fixtures — built on demand by tests/native_modules/build.sh,
# never committed (they are compiled artifacts, not source).
tests/native_modules/*.so
tests/native_modules/*.wasm

# Editor / OS
.DS_Store
*.swp
